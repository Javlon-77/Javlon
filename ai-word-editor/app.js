const CONFIG={API_BASE_URL:"https://word-dkng.onrender.com"};
const $=id=>document.getElementById(id);
const editor=$("editor"),fileInput=$("fileInput"),mediaInput=$("mediaInput"),promptEl=$("prompt"),sendBtn=$("sendBtn"),messages=$("messages"),saveBtn=$("saveBtn"),downloadBtn=$("downloadBtn"),newBtn=$("newBtn"),saveState=$("saveState"),statusText=$("statusText"),fileNameEl=$("fileName"),topFileName=$("topFileName"),dropZone=$("dropZone");
let currentDocumentId="",currentFileName="Yangi AI hujjat.docx",currentFileHandle=null,imageCounter=0,diagramCounter=0;
const mediaMap=new Map();
const api=path=>String(CONFIG.API_BASE_URL||"").replace(/\/+$/g,"")+path;
function status(text){if(statusText)statusText.textContent=text;if(saveState)saveState.textContent=text}
function message(text,role="ai"){if(!messages||!text)return;const el=document.createElement("div");el.className=`bubble ${role}`;el.textContent=String(text);messages.appendChild(el);messages.scrollTop=messages.scrollHeight}
function cleanAiAnswer(text){return String(text||"").replace(/^\s*(Hujjat|Document)\s*:\s*[\s\S]*?(?=(Foydalanuvchi so['’]rovi|Foydalanuvchi|User)\s*:)/i,"").replace(/^\s*(Foydalanuvchi so['’]rovi|Foydalanuvchi|User)\s*:\s*[\s\S]*?(?=(Javob|Answer)\s*:)/i,"").replace(/^\s*(Javob|Answer)\s*:\s*/i,"").trim()}
async function readJson(response){const type=response.headers.get("content-type")||"";if(!type.includes("application/json")){if(!response.ok)throw new Error(`Server xatosi (${response.status})`);return response}const data=await response.json();if(!response.ok)throw new Error(data.error||`Server xatosi (${response.status})`);return data}
function rememberFile(name){currentFileName=name||"Yangi AI hujjat.docx";if(fileNameEl)fileNameEl.textContent=currentFileName;if(topFileName)topFileName.textContent=currentFileName}
function closeFiles(){$("filesPanel")?.classList.remove("open")}
function clearEditor(){editor?.replaceChildren();mediaMap.clear();imageCounter=0;diagramCounter=0}
function appendText(text){if(!editor)return;for(const line of String(text??"").split(/\r?\n/)){const p=document.createElement("div");p.textContent=line;editor.appendChild(p)}}
function setText(text){clearEditor();appendText(text||"")}
function readAsDataUrl(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result||""));r.onerror=()=>reject(r.error||new Error("Rasmni o‘qib bo‘lmadi."));r.readAsDataURL(file)})}
function createImageBlock(id,caption=""){const data=mediaMap.get(id);if(!data)return null;const block=document.createElement("div");block.className="word-media";block.dataset.imageId=id;block.contentEditable="false";const img=document.createElement("img");img.src=data.dataUrl;img.alt=data.name||"Rasm";img.draggable=false;const cap=document.createElement("div");cap.className="word-media-caption";cap.contentEditable="true";cap.spellcheck=true;cap.dataset.placeholder="Rasm ostiga ma’lumot yozing...";cap.textContent=caption;block.append(img,cap);return block}
function diagramStyle(){if($("diagram-runtime-style"))return;const s=document.createElement("style");s.id="diagram-runtime-style";s.textContent=`.ai-diagram{margin:24px auto;padding:24px 18px;border:1px solid #dfe5ee;border-radius:18px;background:#fbfcfe;max-width:760px;text-align:center;font-family:Inter,system-ui,sans-serif}.ai-diagram-title{font-size:18px;font-weight:800;color:#172033;margin-bottom:18px}.ai-diagram-branch{display:flex;flex-direction:column;align-items:center;gap:10px}.ai-diagram-node{min-width:170px;max-width:280px;padding:13px 18px;border:2px solid #3867ff;border-radius:12px;background:#fff;color:#1c2940;font-weight:700;box-shadow:0 6px 18px rgba(56,103,255,.10);outline:none}.ai-diagram-line{width:2px;height:24px;background:#9aa8bb}.ai-diagram-row{display:flex;justify-content:center;gap:18px;flex-wrap:wrap;position:relative}.ai-diagram-row:before{content:"";position:absolute;top:-10px;left:12%;right:12%;height:2px;background:#9aa8bb}.ai-diagram-child{display:flex;flex-direction:column;align-items:center;gap:8px}.ai-diagram-child .ai-diagram-line{height:18px}.ai-diagram-node.child{border-color:#18a878}.ai-diagram-node[contenteditable=true]:focus{box-shadow:0 0 0 3px rgba(56,103,255,.12)}`;document.head.appendChild(s)}
function createDiagramBlock(title="Diagramma",labels=["Asosiy ma’lumot","Kasb","Ta’lim"]){diagramStyle();const wrap=document.createElement("div");wrap.className="ai-diagram";wrap.dataset.diagramId=`diagram-${++diagramCounter}`;wrap.contentEditable="false";const t=document.createElement("div");t.className="ai-diagram-title";t.contentEditable="true";t.textContent=title;const branch=document.createElement("div");branch.className="ai-diagram-branch";const root=document.createElement("div");root.className="ai-diagram-node";root.contentEditable="true";root.textContent=title;const line=document.createElement("div");line.className="ai-diagram-line";const row=document.createElement("div");row.className="ai-diagram-row";for(const label of labels.slice(0,6)){const child=document.createElement("div");child.className="ai-diagram-child";const stem=document.createElement("div");stem.className="ai-diagram-line";const node=document.createElement("div");node.className="ai-diagram-node child";node.contentEditable="true";node.textContent=label;child.append(stem,node);row.appendChild(child)}branch.append(root,line,row);wrap.append(t,branch);return wrap}
function diagramRequest(text){return /\b(diagramma|diagram|sxema|schema|blok.?diagramma|flowchart)\b/i.test(String(text||""))}
function diagramFromPrompt(text){const q=String(text||"");const lower=q.toLowerCase();let title="ODAMLAR";if(/odam|inson|people/.test(lower))title="ODAMLAR";else{const m=q.match(/(?:haqida|to‘g‘risida|togri?sida)\s+(.+)/i);if(m&&m[1])title=m[1].replace(/[.!?]+$/g,"").trim()||"DIAGRAMMA";else title="DIAGRAMMA"}let labels=lower.includes("odam")||lower.includes("inson")?["Erkaklar","Ayollar","Bolalar","Kattalar"]:["Asosiy qism","Ma’lumot","Tafsilot","Natija"];if(/kasb|ish|profession/.test(lower))labels=["Dasturchi","Muhandis","O‘qituvchi","Shifokor"];if(/ta['’]lim|maktab|o‘qish/.test(lower))labels=["Maktab","Universitet","Kurslar","Mustaqil o‘qish"];return createDiagramBlock(title,labels)}
function diagramToText(block){const nodes=[...block.querySelectorAll(".ai-diagram-node")].map(n=>n.innerText.trim()).filter(Boolean);if(!nodes.length)return"";return `┌──────────────────────────┐\n│ ${nodes[0].padEnd(24," ").slice(0,24)} │\n└────────────┬─────────────┘\n             │\n${nodes.slice(1).map(n=>`       ┌──────────────────┐\n       │ ${n.padEnd(16," ").slice(0,16)} │\n       └──────────────────┘`).join("\n")}`}
function serialiseContent(){const out=[];if(!editor)return out;for(const node of [...editor.childNodes]){if(node.nodeType===1&&node.classList.contains("word-media")){const id=node.dataset.imageId,data=mediaMap.get(id);if(data)out.push({type:"image",id,dataUrl:data.dataUrl,caption:node.querySelector(".word-media-caption")?.innerText.trim()||""});continue}if(node.nodeType===1&&node.classList.contains("ai-diagram")){out.push({type:"text",text:diagramToText(node)});continue}out.push({type:"text",text:String(node.innerText??node.textContent??"")})}return out}
function contentForAI(){return serialiseContent().map(x=>x.text||"").join("\n").trim()}
function restoreContent(content,fallbackText=""){clearEditor();if(!Array.isArray(content)||!content.length){setText(fallbackText);return}for(const item of content){if(item?.type==="image"&&item.dataUrl){const id=String(item.id||`img-${++imageCounter}`);mediaMap.set(id,{dataUrl:String(item.dataUrl),name:"Rasm"});const block=createImageBlock(id,item.caption||"");if(block)editor.appendChild(block)}else if(item?.type==="text")appendText(item.text||"")}}
function rebuildFromAI(text){setText(text)}
function openDB(){return new Promise((resolve,reject)=>{const r=indexedDB.open("ai-word-studio",1);r.onupgradeneeded=()=>r.result.createObjectStore("handles");r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function putHandle(id,handle){try{const db=await openDB();await new Promise((resolve,reject)=>{const tx=db.transaction("handles","readwrite");tx.objectStore("handles").put(handle,id);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)});db.close()}catch{}}
async function getHandle(id){try{const db=await openDB();const value=await new Promise((resolve,reject)=>{const tx=db.transaction("handles","readonly");const req=tx.objectStore("handles").get(id);req.onsuccess=()=>resolve(req.result||null);req.onerror=()=>reject(req.error)});db.close();return value}catch{return null}}
async function writablePermission(handle){if(!handle?.queryPermission)return true;let p=await handle.queryPermission({mode:"readwrite"});if(p!=="granted"&&handle.requestPermission)p=await handle.requestPermission({mode:"readwrite"});return p==="granted"}
function downloadBlob(blob,name){const url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}
async function loadRecents(mode="files"){const panel=$("filesPanel"),body=$("filesBody"),title=$("filesTitle");if(!panel||!body)return;title.textContent=mode==="favorites"?"★ Sevimlilar":"📁 Mening fayllarim · Recents";body.innerHTML='<div class="file-loading">Yuklanmoqda...</div>';panel.classList.add("open");try{const data=await readJson(await fetch(api("/api/files"),{cache:"no-store"}));let list=Array.isArray(data.files)?data.files:[];if(mode==="favorites")list=list.filter(x=>x.favorite);list.sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));body.replaceChildren();if(!list.length){body.innerHTML='<div class="file-empty"><b>Hozircha fayl yo‘q</b><span>Word fayl oching — shu yerda Recents ko‘rinadi.</span>';return}for(const item of list){const row=document.createElement("div");row.className="file-card";const icon=document.createElement("div");icon.className="file-icon";icon.textContent="DOCX";const info=document.createElement("div");info.className="file-info";const strong=document.createElement("strong");strong.textContent=item.name;const small=document.createElement("span");small.textContent=new Date(item.updatedAt).toLocaleString("uz-UZ");info.append(strong,small);const actions=document.createElement("div");actions.className="file-actions";const fav=document.createElement("button");fav.type="button";fav.textContent=item.favorite?"★":"☆";fav.onclick=async e=>{e.stopPropagation();try{await readJson(await fetch(api(`/api/files/${encodeURIComponent(item.id)}/favorite`),{method:"POST"}));await loadRecents(mode)}catch{}};const down=document.createElement("button");down.type="button";down.textContent="↓";down.onclick=e=>{e.stopPropagation();window.open(api(`/api/files/${encodeURIComponent(item.id)}/download`),"_blank","noopener")};actions.append(fav,down);row.append(icon,info,actions);row.onclick=()=>openSavedFile(item);body.appendChild(row)}}catch{body.innerHTML='<div class="file-empty"><b>Fayllar yuklanmadi</b><span>Backend ulanishini tekshiring.</span></div>'}}
async function openSavedFile(item){try{status("Fayl ochilmoqda...");const data=await readJson(await fetch(api(`/api/files/${encodeURIComponent(item.id)}`),{cache:"no-store"}));currentDocumentId=data.documentId;rememberFile(data.fileName);currentFileHandle=await getHandle(item.id);if(currentFileHandle&&!(await writablePermission(currentFileHandle)))currentFileHandle=null;restoreContent(data.content,data.text||"");closeFiles();status("Fayl ochildi")}catch(error){status("Xatolik");message(`❌ ${error.message}`)}}
async function chooseWordFile(){try{if(window.showOpenFilePicker){const [handle]=await window.showOpenFilePicker({multiple:false,types:[{description:"Microsoft Word",accept:{"application/vnd.openxmlformats-officedocument.wordprocessingml.document":[".docx"]}}]});currentFileHandle=handle;return await handle.getFile()}fileInput?.click();return null}catch(error){if(error?.name!=="AbortError")message("❌ Word faylini tanlab bo‘lmadi.");return null}}
async function uploadWord(file){if(!file)return;try{const form=new FormData();form.append("file",file);status("Word ochilmoqda...");const data=await readJson(await fetch(api("/api/extract"),{method:"POST",body:form}));currentDocumentId=data.documentId;rememberFile(data.fileName||file.name);if(currentFileHandle)await putHandle(currentDocumentId,currentFileHandle);setText(data.text||"");closeFiles();status("Word ulandi");message(`✅ ${file.name} ochildi.`)}catch(error){status("Word xatosi");message(`❌ ${error.message}`)}}
function setupWord(){$("wordBtn")?.addEventListener("click",async e=>{e.preventDefault();const file=await chooseWordFile();if(file)await uploadWord(file)});fileInput?.addEventListener("change",async()=>{const file=fileInput.files?.[0];if(file){currentFileHandle=null;await uploadWord(file)}fileInput.value=""})}
async function saveWord(){if(!currentDocumentId){message("⚠️ Avval Word faylini oching.");return}status("Saqlanmoqda...");if(saveBtn)saveBtn.disabled=true;try{const content=serialiseContent();const data=await readJson(await fetch(api("/api/save"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({documentId:currentDocumentId,content})}));const r=await fetch(api(`/api/files/${encodeURIComponent(currentDocumentId)}/download`),{cache:"no-store"});if(!r.ok)throw new Error("Yangilangan Word faylni olishda xato.");const blob=await r.blob();if(currentFileHandle){try{if(!(await writablePermission(currentFileHandle)))throw new Error("Faylga yozish ruxsati berilmadi.");const w=await currentFileHandle.createWritable();await w.write(blob);await w.close()}catch{downloadBlob(blob,currentFileName);message("⚠️ Original Word fayli bloklangan. Yangilangan nusxa yuklab olindi.")}}else downloadBlob(blob,currentFileName);status("Saqlandi");message(`💾 ${data.fileName} saqlandi.`);loadRecents("files").catch(()=>{})}catch(error){status("Saqlash xatosi");message(`❌ ${error.message}`)}finally{if(saveBtn)saveBtn.disabled=false}}
async function addImage(file){if(!file?.type?.startsWith("image/")){message("❌ Faqat rasm fayli tanlang.");return}try{const dataUrl=await readAsDataUrl(file),id=`img-${++imageCounter}-${Date.now()}`;mediaMap.set(id,{dataUrl,name:file.name});const block=createImageBlock(id);if(block){editor.appendChild(block);block.querySelector(".word-media-caption")?.focus();status("Rasm qo‘shildi");message(`🖼️ ${file.name} qo‘shildi.`)}}catch(error){message(`❌ ${error.message}`)}}
function setupMedia(){const open=()=>mediaInput?.click();$("mediaBtn")?.addEventListener("click",e=>{e.preventDefault();open()});$("chatAttach")?.addEventListener("click",e=>{e.preventDefault();open()});mediaInput?.addEventListener("change",()=>{[...(mediaInput.files||[])].forEach(addImage);mediaInput.value=""})}
async function sendPrompt(text=promptEl?.value){const instruction=String(text||"").trim();if(!instruction)return;message(instruction,"user");if(promptEl)promptEl.value="";if(sendBtn)sendBtn.disabled=true;try{if(diagramRequest(instruction)){const block=diagramFromPrompt(instruction);editor?.appendChild(block);status("Diagramma tayyor");message("✅ To‘rtburchak diagramma tayyorlandi. Blok ichidagi yozuvlarni bosib o‘zgartirishingiz mumkin.");return}status("AI ishlayapti...");const data=await readJson(await fetch(api("/api/chat"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({documentId:currentDocumentId,documentText:contentForAI(),instruction})}));if(data.changed&&data.editedDocument)rebuildFromAI(cleanAiAnswer(data.editedDocument));if(data.changed)status("AI tahrirladi");const answer=cleanAiAnswer(data.answer);if(answer)message(answer)}catch(error){status("AI xatosi");message(`❌ ${error.message}`)}finally{if(sendBtn)sendBtn.disabled=false}}
function setupChat(){sendBtn?.addEventListener("click",e=>{e.preventDefault();sendPrompt()});promptEl?.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();sendPrompt()}});document.querySelectorAll("[data-prompt]").forEach(b=>b.addEventListener("click",()=>sendPrompt(b.dataset.prompt)))}
async function exportWord(){if(!currentDocumentId){message("⚠️ Avval Word faylini oching.");return}try{const r=await readJson(await fetch(api("/api/export"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({documentId:currentDocumentId,content:serialiseContent()})}));downloadBlob(await r.blob(),currentFileName);message(`✅ ${currentFileName} yuklab olindi.`)}catch(error){message(`❌ ${error.message}`)}}
function setupToolbar(){document.querySelectorAll(".toolbar-left .tool").forEach(button=>button.addEventListener("click",e=>{e.preventDefault();const cmd=button.dataset.cmd;if(!cmd)return;editor?.focus();try{document.execCommand(cmd,false,null)}catch{}status("O‘zgartirildi")}))}
function setupEditor(){editor?.addEventListener("input",()=>status("O‘zgartirildi"));if(!dropZone||!editor)return;editor.addEventListener("dragover",e=>{e.preventDefault();dropZone.style.display="grid"});editor.addEventListener("dragleave",()=>dropZone.style.display="none");editor.addEventListener("drop",e=>{e.preventDefault();dropZone.style.display="none";[...(e.dataTransfer?.files||[])].filter(f=>f.type.startsWith("image/")).forEach(addImage)})}
function setupNavigation(){const buttons=[...document.querySelectorAll(".nav button")];buttons.forEach(button=>button.addEventListener("click",e=>{e.preventDefault();buttons.forEach(x=>x.classList.remove("active"));button.classList.add("active");const text=button.textContent||"";if(/Mening fayllarim|Yaqinda/i.test(text))loadRecents("files");else if(/Sevimlilar/i.test(text))loadRecents("favorites");else if(/Bosh sahifa/i.test(text))closeFiles()}));$("filesTopBtn")?.addEventListener("click",e=>{e.preventDefault();loadRecents("files")});$("closeFiles")?.addEventListener("click",e=>{e.preventDefault();closeFiles()})}
function setupNew(){newBtn?.addEventListener("click",e=>{e.preventDefault();clearEditor();currentDocumentId="";currentFileHandle=null;rememberFile("Yangi AI hujjat.docx");closeFiles();status("Yangi hujjat")})}
async function checkBackend(){try{const data=await readJson(await fetch(api("/health"),{cache:"no-store"}));status(data.aiConfigured?"AI ulangan":"AI kaliti sozlanmagan")}catch{status("Backend ulanmagan")}}
function init(){setupNavigation();setupWord();setupMedia();setupToolbar();setupEditor();setupChat();setupNew();saveBtn?.addEventListener("click",e=>{e.preventDefault();saveWord()});downloadBtn?.addEventListener("click",e=>{e.preventDefault();exportWord()});checkBackend()}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init);else init();